## What changed

<!-- One or two sentences. Link the issue: "Fixes #NN" closes it on merge. -->

Fixes #

## Why

<!-- Motivation / which roadmap step or v2 phase this belongs to. -->

## Test evidence

<!-- Paste the pytest command + result, or explain why no tests apply. -->

```
pytest tests/
```

## Checklist

- [ ] CI green (`test` + `lint`)
- [ ] Docs updated if behavior changed (`CLAUDE.md`, `docs/`)
- [ ] No stray experiment artifacts (`result/`, checkpoints) in the diff
