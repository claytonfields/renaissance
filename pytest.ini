[pytest]
testpaths = tests
# test_trainer.py and test_datasets.py fail at collection due to a
# tensorflow/numpy incompat (np.object removed in numpy 1.20+). They'll
# come back online when the legacy data layer is rewritten.
addopts = -v --tb=short --ignore=tests/test_trainer.py --ignore=tests/test_datasets.py
