[pytest]
testpaths = tests
# test_datasets.py fails at collection: the legacy data layer imports
# transformers.data, which pulls in tensorflow and hits the np.object
# removal (numpy 1.20+). It comes back online when the legacy data
# layer is rewritten.
addopts = -v --tb=short --ignore=tests/test_datasets.py
